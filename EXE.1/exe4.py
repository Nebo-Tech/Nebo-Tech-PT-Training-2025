# Importing math module for π and accepting user input for radius
import math
radius = float(input("Enter the radius: "))

# Formula for circle area: πr²
area = math.pi * radius**2
print("Area =", area)