# Loop through rows from 1 to 5
for row in range(1, 6):
    
    # For each row, print numbers starting from 1 up to the current row number
    for num in range(1, row + 1):
        print(num, end=" ")  # Print number and stay on same line with space
    print()  # Move to the next line after each row is printed